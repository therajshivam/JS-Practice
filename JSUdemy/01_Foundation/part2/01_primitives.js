/*
DataTypes - primitives and non-primitives
1. Primitives - number, string, boolean, bigint, undefined, null, symbol
2. Non-primitives - objects (objects, arrays, functions)
*/

// Number 
let balance = 120
console.log(balance);
console.log(typeof(balance));

let anotherBalance = new Number(120)
console.log(anotherBalance.valueOf());
